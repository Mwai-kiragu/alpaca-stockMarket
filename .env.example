# Server Configuration
PORT=3000
NODE_ENV=development

# Database PostgreSQL
# Option 1: Use DATABASE_URL (recommended)
DATABASE_URL=postgresql://postgres:@Mwai1234@localhost:5433/trading_platform

# Option 2: Or use individual connection parameters
DB_HOST=localhost
DB_PORT=5433
DB_NAME=trading_platform
DB_USER=postgres
DB_PASSWORD=Mwai1234
DB_SSL=false

# JWT Configuration
JWT_SECRET=your-super-secure-jwt-secret-key
JWT_EXPIRES_IN=7d

# Alpaca API Configuration
ALPACA_API_KEY=your-alpaca-api-key
ALPACA_SECRET_KEY=your-alpaca-secret-key
ALPACA_BASE_URL=https://paper-api.alpaca.markets
ALPACA_DATA_BASE_URL=https://data.alpaca.markets

# MPesa Daraja API Configuration
MPESA_CONSUMER_KEY=your-mpesa-consumer-key
MPESA_CONSUMER_SECRET=your-mpesa-consumer-secret
MPESA_PASSKEY=your-mpesa-passkey
MPESA_SHORTCODE=your-mpesa-shortcode
MPESA_CALLBACK_URL=https://yourdomain.com/api/v1/wallet/mpesa/callback

# Exchange Rate API
EXCHANGE_RATE_API_KEY=your-exchange-rate-api-key

# Email Configuration (for notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-email-password

# Redis Configuration (for caching, sessions, and real-time notifications)
# Option 1: Use REDIS_URL (recommended)
REDIS_URL=redis://localhost:6379

# Option 2: Or use individual connection parameters
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Firebase Configuration (for push notifications)
# Create a Firebase project at https://console.firebase.google.com/
# Download your service account JSON from Project Settings > Service Accounts
# Then stringify the JSON and paste it here (or use a file path)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project-id","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}

# Encryption
ENCRYPTION_KEY=your-32-character-encryption-key